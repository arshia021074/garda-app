name: garda_app
description: Garda Shahanshahi Messenger
version: 1.0.0

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^2.0.3
  encrypt: ^5.0.3

flutter:
  uses-material-design: true
  assets:
    - assets/tehran_background.jpg
